# Compaction prompt — paste this after `/compact` in THIS window

> Purpose: shrink this window's context while preserving everything the
> follow-on adversarial review needs. After compacting, this same assistant
> continues and runs the review. The authoritative state lives in two files on
> disk that survive compaction untouched — this prompt makes sure the summary
> keeps the pointers and a minimal spine so re-reading them is fast.
>
> Usage: `/compact <paste everything below the line>`

---

Summarize this conversation for a continuing session whose SOLE next job is to
adversarially review and repair the Multipaste **v2.4.0** change, then keep
going to ship quality (boil the ocean).

CRITICAL — preserve these facts VERBATIM; do not paraphrase away specifics:

- **Repo/branch:** `/Users/newdldewdl/code/multipaste`, on branch
  `feature/plain-text-paste` (HEAD commit `f92645a`; the code change is commit
  `dcc33a8`; `f92645a` is a docs-only roadmap add). Parent `main` = v2.3.0.
  The change is **built, green (288 tests), DMG-ready, and NOT publicly
  shipped** (not merged, no GitHub release, Homebrew cask not bumped).
- **The two on-disk anchor files are authoritative and survive this
  compaction — the continuing session MUST re-read them before doing
  anything:**
  - `docs/reviews/v2.4.0-COMPACTION-BRIEF.md` — the full state of the change
    (what/why, every file touched, the new Core types, the deliberate design
    decisions, the test baseline, ground-truth commands).
  - `docs/reviews/v2.4.0-REVIEW-PROMPT.md` — the exact review task to execute
    (reverse-prompt across correctness / security / edge-cases / thread-safety
    / api-design-testability / backward-compat / test-mutation / macOS-
    pasteboard / docs; verify each finding against the real code per CoVe; fix
    every confirmed defect with a regression test in the custom harness style,
    mutation-tested; deliverable is a findings report + patches, all re-run
    green).
- **What v2.4.0 is (spine):** `⇧↩` in the picker pastes plain text (strips
  RTF). Pure Core policy `MultipasteCore/PasteFlavor.swift` (`PasteFlavor`,
  `PasteWrite`, `PlainText.string` / `PlainText.pasteWrite`); `Paster.put`
  is a thin executor over it (`.rich` default = pre-2.4.0 behavior);
  flavor threads through `PickerWindow.effectiveFlavor` → `onPick` →
  `AppDelegate` `pickAndPaste`/`deliver`/`deliverSequentially`/
  `pasteSequentially`. Optional `Preferences.plainTextPasteDefault` (default
  false). Bundled bugfix: `HistoryStore.insert` now inherits a re-copied
  item's snippet `trigger`. Tests: `PlainTextTests` (13), `HistoryStore`
  reCopy* (2), `Preferences` plainTextPasteDefault* (2). Smoke:
  `scripts/plaintext-paste-smoke-test.swift`.
- **Ground-truth commands** the continuing session should run first:
  `cd ~/code/multipaste && make test` (expect 288 passed) and
  `swift scripts/plaintext-paste-smoke-test.swift` (expect exit 0).
- **Operating standard (keep):** CoVe is ON (verify every load-bearing/success
  claim against ground truth by re-running and reading exit codes; never trust
  a single rendered output). Custom test harness, NOT XCTest
  (`enum XTests{registerAll()→TestRegistry.register("Suite/name",fn)}`,
  `try expect`/`expectEqual`, register in `Tests/MultipasteCoreTests/main.swift`;
  only `MultipasteCore` is unit-testable). Ethos: no telemetry, no new deps, no
  redesign, macOS 13+. Never use em dashes. Boil the ocean: ship the finished
  product with tests + docs, mutation-test new tests, don't table or hand off.
- **Known likely review targets** (do not pre-judge; verify): `effectiveFlavor`
  is pure logic trapped in AppKit `PickerWindow` and currently has NO unit test
  (candidate to extract to Core); the `.rich` default on `Paster.put` as a
  possible footgun; files-pasted-plain exposes a full path; the dead
  `HistoryStore` `DispatchQueue`; docs/coverage-table arithmetic accuracy.

You MAY drop, as recoverable-from-disk-or-git: the blow-by-blow tool history,
the earlier 8-lens improvement swarm's raw output (its result is committed at
`docs/IMPROVEMENT-ROADMAP.md`), the memory-index re-lean steps, and the
Fable-5-new-window discussion (superseded: the review now runs in THIS window).

End the summary with: "Next action: re-read docs/reviews/
v2.4.0-COMPACTION-BRIEF.md and v2.4.0-REVIEW-PROMPT.md, then execute the review."
