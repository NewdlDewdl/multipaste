# Prompts: compact this window, then build + ship the ⌘1-9 recent-rail fix

> **EXECUTED 2026-07-04, same session, without compaction.** v2.4.1 is
> shipped (release + cask + local install) and the acceptance tests
> passed. These prompts are kept as the worked example of the pattern;
> do NOT run them again.

Paste PROMPT 1 as the argument to `/compact`; after compaction, paste
PROMPT 2. The durable state lives in
`docs/reviews/v2.4.1-QUICKPICK-BRIEF.md` (committed on
`fix/quickpick-recent-rail`), so the summary only needs pointers and the
authorization.

---

## PROMPT 1 (give to /compact)

Summarize this conversation for a continuing session whose SOLE next job
is to BUILD AND SHIP Multipaste v2.4.1: retarget ⌘1-9 quick-pick (picker
badges + picker handler + menu keyEquivalents) from the pinned-first
display order to the first 9 UNPINNED items, then publish the release
and upgrade this Mac.

CRITICAL, preserve VERBATIM:
- Rohin AUTHORIZED the full ship in his request ("Ship the complete
  thing"): merge to main, push, GitHub release v2.4.1, Homebrew cask
  bump, brew upgrade + app relaunch. Do not re-ask permission.
- Repo: /Users/newdldewdl/code/multipaste, work branch
  fix/quickpick-recent-rail (off main @ 5a6b2a9, 302 tests green,
  installed app = 2.4.0). The authoritative anchor the continuing
  session MUST read before acting: docs/reviews/v2.4.1-QUICKPICK-BRIEF.md
  (CoVe-verified ground truth: current behavior is label-consistent but
  spends digits on pinned rows; required end state; Core-policy
  architecture requirement; live acceptance recipe; version-bump
  quintet; ship steps). Generic runbook: docs/RELEASING.md. Pasteboard
  oracle tool: scripts/pbtool.swift.
- IMPORTANT premise correction baked into the brief: live tests proved
  labels and actions currently AGREE (both pinned-first); the fix is a
  deliberate retarget to the unpinned rail, not a desync repair. Honor
  the brief's do-not-do list (mark badges untouched, pinned-first
  display order untouched, no em dashes, no telemetry/deps, macOS 13+,
  custom harness not XCTest).
- Operating standard: CoVe is ON (exit codes, re-downloaded asset
  hashes, Info.plist reads, pasteboard-type oracle + log lines for live
  claims). Mutation-test every new test. Boil the ocean: finished
  product with tests + docs, extract learnings (RELEASING.md gotchas,
  memory updates), never table anything.

You MAY drop as recoverable from disk/git: the v2.4.0 ship blow-by-blow,
the memory-index re-lean, and all investigation transcript detail (the
brief has the verified summary).

End the summary with: "Next action: cd ~/code/multipaste && git checkout
fix/quickpick-recent-rail, read docs/reviews/v2.4.1-QUICKPICK-BRIEF.md,
then build the fix."

---

## PROMPT 2 (paste after compaction)

Build and ship Multipaste v2.4.1 now, in this window. Rohin already
authorized the outward-facing steps (merge, push, GitHub release, cask
bump, brew upgrade); execute them, do not re-ask. First: cd
~/code/multipaste && git checkout fix/quickpick-recent-rail, read
docs/reviews/v2.4.1-QUICKPICK-BRIEF.md fully, then execute it exactly:
(1) pure Core quick-pick policy (digit -> Nth unpinned item; per-row
digit labels, nil for pinned) with unit tests covering all-pinned /
none-pinned / mixed / >9 / filtered / empty, mutation-tested per the
repo rule; (2) wire the picker badge, picker ⌘digit handler, and menu
keyEquivalents to that ONE policy so the surfaces cannot drift; (3)
version-bump quintet to 2.4.1 (CFBundleVersion 25) + README ⌘1-9 docs +
CHANGELOG entry; (4) make test / make plaintext-smoke-test / make
smoke-test / make verify-app all exit 0 with the new count reported
honestly; (5) ship per docs/RELEASING.md: dmg + sha256 (one file, never
rebuild after hashing), merge --no-ff to main, re-gate on main, push,
gh release create v2.4.1 with the CHANGELOG body, re-download the asset
and compare sha256, cask bump commit "multipaste 2.4.1" + push (keep
depends_on macos: :ventura symbol form), brew update && brew upgrade
--cask multipaste, QUIT AND RELAUNCH the app and prove the new version
via the log boot line and trust=ON; (6) run the brief's live acceptance
test headlessly (pbtool + synthetic keys: with pinned items present, ⌘1
must paste the FIRST UNPINNED item, badge screenshot shows ⌘1 on that
row and none on pinned rows, menu ⌘1 agrees) and confirm
releases/latest == v2.4.1; (7) aftercare: update
~/.claude/projects/-/memory/reference_multipaste_app.md (v2.4.1 shipped,
new sha, quick-pick semantics), add any new gotcha to docs/RELEASING.md,
push any doc commits, and sweep the session for reusable extractions.
If any step fails, fix the root cause permanently and continue. Report
the outcome with real numbers: test count + exit codes on main, release
URL + asset hash, cask commit, installed version, live-acceptance
evidence, and anything you could not verify. Boil the ocean.
