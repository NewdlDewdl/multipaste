# Ship prompts: compact this window, then ship v2.4.0

Two prompts for the same window. Paste PROMPT 1 as the argument to
`/compact`; after compaction completes, paste PROMPT 2 as the next
message. The durable state lives in `docs/reviews/v2.4.0-SHIP-BRIEF.md`
and `docs/RELEASING.md` (both committed), so a lossy summary cannot lose
the ship data; the prompts only need to preserve the pointers and the
authorization.

---

## PROMPT 1 (give to /compact)

Summarize this conversation for a continuing session whose SOLE next job
is to SHIP Multipaste v2.4.0: publish it on GitHub completely and update
the installed app on this Mac, then extract every reusable learning.

CRITICAL, preserve VERBATIM:
- Rohin EXPLICITLY AUTHORIZED the outward-facing ship in his final
  message: merge to main, push, create the GitHub release, bump the
  Homebrew cask, and upgrade the installed app. Do not re-ask permission
  for those specific actions; they are the task.
- Repo: /Users/newdldewdl/code/multipaste, ship branch
  review/v2.4.0-fable-audit (contains feature/plain-text-paste). main is
  still at v2.3.0. The installed /Applications/Multipaste.app is 2.3.0.
- The two on-disk anchors are authoritative and survive compaction; the
  continuing session MUST read them before acting:
  docs/reviews/v2.4.0-SHIP-BRIEF.md (verified ship inputs: DMG sha256
  59522ee530c78b47f07ba24a159deba0ff203580cc439f4ce4f6c3a239473f1d, cask
  location, release-title convention, the exact 10-step sequence, the
  do-not-do list) and docs/RELEASING.md (the permanent runbook). Context
  on what is being shipped: docs/reviews/v2.4.0-FINDINGS.md (the
  adversarial review: 302 tests, layered smoke gates, all fixes
  mutation-tested).
- Ground-truth gates to run first: cd ~/code/multipaste && git checkout
  review/v2.4.0-fable-audit && make test (expect 302 passed) && make
  plaintext-smoke-test && make verify-app, reading exit codes.
- Operating standard: CoVe is ON (verify every success claim against
  ground truth: exit codes, re-downloaded asset hashes, Info.plist reads,
  the GitHub API; never trust one rendered output). Never use em dashes
  (U+2014). Custom test harness, NOT XCTest. Ethos: no telemetry, no new
  deps, macOS 13+. Boil the ocean: ship the finished product, fix
  problems permanently en route, document learnings (memory files +
  RELEASING.md gotchas), never table anything.

You MAY drop as recoverable from disk/git: the adversarial review
blow-by-blow (docs/reviews/v2.4.0-FINDINGS.md has it), the swarm
transcripts, the prompt-authoring history, and all pre-review state.

End the summary with: "Next action: read
docs/reviews/v2.4.0-SHIP-BRIEF.md and docs/RELEASING.md, then execute
the ship."

---

## PROMPT 2 (paste after compaction)

Ship Multipaste v2.4.0 now, in this window. Rohin has already authorized
the outward-facing steps (merge, push, GitHub release, cask bump, local
brew upgrade); execute them, do not re-ask. First: cd ~/code/multipaste
&& git checkout review/v2.4.0-fable-audit, read
docs/reviews/v2.4.0-SHIP-BRIEF.md and docs/RELEASING.md, then run the
brief's 10-step sequence exactly, CoVe-verifying every gate (exit codes;
re-download the uploaded release asset and confirm its sha256 equals
59522ee530c78b47f07ba24a159deba0ff203580cc439f4ce4f6c3a239473f1d; read
the installed Info.plist after brew upgrade; confirm the GitHub
releases/latest API returns v2.4.0). Respect the brief's do-not-do list
(never rebuild the DMG after hashing; no tag rewrites; no em dashes). If
any step fails, fix the root cause permanently, add the gotcha to
docs/RELEASING.md, and continue. After the ship verifies: run the 5-step
live manual checklist from docs/reviews/v2.4.0-FINDINGS.md as far as
synthetic input allows and report what remains truly manual; update
~/.claude/projects/-/memory/reference_multipaste_app.md from
not-yet-shipped to shipped-and-installed (new sha, date, release URL);
sweep the session for anything else worth extracting (new gotchas into
RELEASING.md, memory updates, skill updates to
~/.claude/skills/codebase-improvement-swarm if the ship taught the
pattern anything); then report the outcome with the real numbers: test
count and exit codes on main, release URL and asset hash, cask commit,
installed version, and anything you could not verify. Boil the ocean.
